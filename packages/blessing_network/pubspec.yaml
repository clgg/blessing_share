name: blessing_network
description: Dio + Retrofit network infrastructure for Blessing Share (DDD infrastructure layer).
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  dio: ^5.7.0
  json_annotation: ^4.9.0
  retrofit: ^4.4.1

dev_dependencies:
  build_runner: ^2.4.11
  json_serializable: ^6.8.0
  lints: ^4.0.0
  retrofit_generator: ^9.1.2
  test: ^1.25.2
